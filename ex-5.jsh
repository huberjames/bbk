
// revise how to put methods into classes, page 7 of the notes

class Point {
	double x;
	double y;
}

Point upLeft = new Point();
upLeft.x = // some input;
upLeft.y = // some input;

Point downRight = new Point();
downRight.x = // some input;
downRight.y =  // some input;


// distanceBetweenPoints is sqrt((x1 − x2)^2 + (y1 − y2)^2)

double distanceBetweenPoints(int downRight.x, int downRight.y, int upLeft.x, int upLeft.y) {
	


} 